<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.studyagent.mapper.SysRoleMapper">

    <select id="selectRolesByUserId" resultType="com.studyagent.entity.SysRole">
        SELECT r.* 
        FROM sys_role r 
        INNER JOIN sys_user_role ur ON r.id = ur.role_id 
        WHERE ur.user_id = #{userId} AND r.del = 0
        ORDER BY r.sort
    </select>

    <select id="selectMenuIdsByRoleId" resultType="java.lang.Long">
        SELECT menu_id FROM sys_role_menu WHERE role_id = #{roleId}
    </select>

    <select id="selectUserCountByRoleId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM sys_user_role WHERE role_id = #{roleId}
    </select>

    <insert id="insertRoleMenus">
        INSERT INTO sys_role_menu (role_id, menu_id, create_time) VALUES
        <foreach collection="menuIds" item="menuId" separator=",">
            (#{roleId}, #{menuId}, NOW())
        </foreach>
    </insert>

    <delete id="deleteRoleMenusByRoleId">
        DELETE FROM sys_role_menu WHERE role_id = #{roleId}
    </delete>

    <delete id="deleteUserRolesByRoleId">
        DELETE FROM sys_user_role WHERE role_id = #{roleId}
    </delete>

</mapper>
